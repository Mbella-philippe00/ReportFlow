import { AiActionPage } from './AiActionPage';

export function WorkflowRecommendationPage() {
    return (
        <AiActionPage
            action="workflow_recommendation"
            description="Recommend a workflow approval decision using the existing AI backend policy."
            title="Workflow Recommendation"
        />
    );
}
